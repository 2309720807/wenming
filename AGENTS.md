# AGENTS.md — 项目规则文件

本文件是项目 `wenming` 的唯一规则依据。**所有编辑者（人类、AI Agent、代码编辑器辅助）的每次改动都必须在本规则框架内进行。**

---

## 0. 最高规则（不可违反）

1. **修改本规则文件（AGENTS.md）必须事先获得用户的明确许可**，未经许可的任何规则修改都属于违规，应立即回退。
2. 用户（项目所有者）的指令优先级高于本文件中的一切规则。
3. 规则冲突时，先停下向用户询问，不得自行取舍。

---

## 1. 项目信息

- 引擎：Godot 4.7（GDScript），Forward Plus 渲染，Jolt 物理，Windows D3D12
- 项目根目录：`res://`（即本文件夹）
- 插件：`addons/agent/`（AlphaAgent AI 插件，**已被 git 忽略，不得提交**）
- 游戏资源目录结构（约定）：
  - `scenes/` — 游戏场景
  - `scripts/` — GDScript 脚本
  - `assets/` — 美术/音频资源
  - `data/` — 数据文件（JSON 等）
  - `docs/design/` — 游戏设计文档（`game_design.md`，设计的唯一依据）

## 2. 改动流程（每次改动必须遵守）

1. **先了解再动手**：读取相关文件、搜索现有实现，不重复造轮子。
2. **遵守既有约定**：模仿相邻文件的代码风格、命名、结构。
3. **询问确认**：对需求不确定、或改动涉及多处时，先向用户确认再执行。
4. **小步提交**：功能按模块拆分，每个可独立验证的改动单独提交。
5. **改动后总结**：列出所有修改/新建的文件路径。
6. **设计先行**：功能实现前先核对 `docs/design/game_design.md`；若实现超出设计范围，先更新设计文档并经用户确认。

## 3. 代码规范（GDScript）

- 使用 4 空格缩进；变量/函数命名遵循 Godot 官方风格（`snake_case`）。
- 函数参数必须带类型声明（如 `func move(speed: float) -> void`）。
- 脚本应有适量易读的注释，说明"为什么"而非"是什么"。
- 不在场景内嵌脚本（`_process` 等逻辑写在外部 `.gd` 文件），除非用户明确要求。
- 路径一律使用 `res://` 绝对路径。
- 不硬编码魔法数字，使用 `@export` 或常量。

## 4. Git 规范

- 分支：`main`；提交信息用中文，格式：`类型: 简述`（类型：`feat`/`fix`/`chore`/`refactor`/`docs`）。
- **提交前必须 `git pull`** 同步朋友的最新改动，避免冲突。
- **禁止提交**：`addons/`（含插件与 API 配置）、`.godot/`、任何 API Key / 密钥 / 个人信息。
- 大改动（重构、多文件改动）先在用户确认后再提交。
- 提交时只包含本次改动相关文件。
- **云端同步需用户授权（重要）**：
  - 本地保存（`git commit`）**随时可进行，无需确认**，属于安全的本机存档。
  - 云端下载（`git pull`）与云端上传（`git push`）**必须经用户明确同意后才能执行**，任何编辑者不得未经允许自行连接云端。
  - 工程**开始**：用户确认开工后，才执行 `git pull` 从云端更新到最新版本。
  - 工程**结束**：用户确认收工后，才执行 `git push` 将改动上传至云端。
  - 目的：多人协作时防止本地与云端工程混淆、避免误覆盖他人改动。

## 5. 禁止事项

- 未经许可修改 `AGENTS.md`。
- 向仓库提交插件配置、密钥、隐私数据。
- 创建无意义的变量名、无用文件、临时调试代码残留。
- 未经用户同意删除或重命名文件。

## 6. 日常协作流程（用户与朋友共同开发）

1. 开工：**用户确认开工后**，运行 `start.bat`（从云端拉取最新代码）
2. 开发：在 Godot 中修改并测试；可随时用 `save_local.bat` 做本地存档（无需确认）
3. 收工：**用户确认收工后**，运行 `push.bat`（提交并推送到云端）

---

*本规则文件所有条目均需用户许可方可修改。*
