# Godot 4+ specific ignores
.godot/
/android/

# AlphaAgent 插件（含模型 API 配置，不纳入版本控制）
/addons/

# 增量备份工作树（独立 git 仓库，不纳入主仓库）
/wenmingzengliang/

# 协作工具工作日志（.workbuddy 会话记忆，不入库）
/.workbuddy/

# UI 截图检查临时文件夹（仅 Agent 检查用，使用完立即清理，不入库）
/screenshots/*
!/screenshots/.gitkeep
