# 项目 GitHub 上传 / 下载规则说明（协作必读）

> 本文档是《文明模拟器》项目多人协作的**云端操作说明书**，请朋友（协作者）务必通读。
> 更详细的规则依据见 `AGENTS.md`（项目唯一规则文件，未经许可不得修改）。

---

## 1. 基本概念

| 概念 | 说明 |
| ---- | ---- |
| 云端仓库 | GitHub：`https://github.com/2309720807/wenming`，分支固定为 `main` |
| 本地仓库 | 你自己电脑上的项目文件夹（`F:\software\Godot\project\wenming` 或你自己克隆的位置） |
| 下载 | `git pull` — 把云端的改动拉到本地（**必须确认后才能做**） |
| 上传 | `git push` — 把本地改动推到云端（**必须确认后才能做**） |
| 本地存档 | `git commit` — 只存到自己电脑，不碰云端（**随时可以做，无需确认**） |
| 占用锁 | `LOCK.md` — 云端上的一个标记文件，谁持有锁谁才能改项目 |

**核心原则：云端不是自动同步的。任何 pull / push 都必须先确认，防止两人互相覆盖。**

---

## 2. 三个一键脚本（协作全靠它们）

项目根目录提供了 3 个 `.bat` 脚本，**请只通过这些脚本进行云端操作，不要手动敲 git 命令**：

| 脚本 | 作用 | 何时用 |
| ---- | ---- | ------ |
| `start.bat` | **开工**：检查云端锁 → 同步云端最新 → 建立占用锁 | 每次开始修改项目前 |
| `save_local.bat` | **本地存档**：只 commit 不推送 | 工作中随时想存一版 |
| `push.bat` | **收工**：提交并推送所有改动 → 释放占用锁 | 每次结束工作时 |

> 为什么强制用脚本？因为仓库装了 `pre-push` 钩子：**绕过脚本直接 `git push` 会被拦截**（防止误推、防止覆盖朋友改动）。

---

## 3. 开工流程（start.bat 做了什么）

每次开始工作，双击 `start.bat`，它会按顺序执行：

1. **检查云端占用锁**（只查看，不合并）：
   - 云端有锁且属于**别人** → 脚本拒绝开工，告诉你被谁占用。**此时禁止推送任何本地改动**（会污染别人的工作期）。
   - 云端有锁且属于**你自己** → 说明上次没收工，继续编辑即可（锁不用重建）。
   - 云端没锁 → 进入下一步。
2. **检查本地是否有未上传的改动**：
   - 有未推送改动，且云端没被别人改过 → 询问你是否"迷你收工"（先把本地改动传上去再继续），**必须你亲口确认**才推。
   - 有未推送改动，但云端已被别人改过 → **禁止推送**（会产生冲突）。此时会自动备份你的本地改动并回退到云端版本，你需要与占用者协调后重新开工。
   - 本地干净 → 直接同步云端最新。
3. **建立占用锁**：写入 `LOCK.md`（含设备标识 + 用户名 + 时间戳）并上传。**锁生效后你才正式拥有编辑权。**

---

## 4. 收工流程（push.bat 做了什么）

每次结束工作，双击 `push.bat`：

1. 提交本地所有改动（自动填写说明）。
2. 上传到云端。
3. **删除并上传释放占用锁 `LOCK.md`** —— 这样朋友下次开工才能拿到锁。

> **收工不释放锁 = 别人永远无法开工。** 即使改了一半，也要跑一遍 `push.bat`。

---

## 5. 占用锁 LOCK.md 详解

- **锁绑定设备**：谁建的锁归谁。你的锁存在时你能编辑，朋友的锁存在时朋友能编辑，**同一时间只能有一方开工**。
- **锁超时**：锁超过 **24 小时**视为失效。若你想接管，必须先与占用者确认，然后删除旧锁重建。
- **开工第一步必须是查锁，而不是看本地改动**：云端有别人的锁时，你本地改动一律不得上传。

---

## 6. 提交规范（commit 信息）

- 信息用**中文**，格式：`类型: 简述`
- 类型：`feat`（新功能）/ `fix`（修 bug）/ `chore`（杂务）/ `refactor`（重构）/ `docs`（文档）
- 示例：`feat: 增加英雄射击功能`、`fix: 修复子弹穿墙`

---

## 7. 禁止事项（违反会被钩子拦截）

- 提交 `addons/`（AI 插件及其 API 配置）、`.godot/`（引擎缓存）。
- 提交任何 API Key、密钥、个人信息。
- 未经项目所有者许可修改 `AGENTS.md`。
- 绕过 `start.bat` / `push.bat` 直接操作 git（钩子会拦）。
- 删除或重命名未确认的文件。

---

## 8. 常见问题（FAQ）

**Q：我直接 `git push` 报错被拦截，为什么？**
A：这是 `pre-push` 钩子在保护云端。非锁内容的推送必须由 `push.bat`（收工流程）放行，请用脚本操作。

**Q：开工时提示"云端有别人的锁"，怎么办？**
A：等朋友收工（释放锁），或与朋友确认后接管（锁超 24 小时才可自行处理）。

**Q：本地有改动但云端被别人改过了，怎么处理？**
A：脚本会自动备份你的改动 → 回退到云端版本。之后与朋友协调，重新开工再合入你的改动。

**Q：我想先存一版，不想传云端，可以吗？**
A：可以，随时双击 `save_local.bat`，只存在自己电脑，安全。

**Q：收工后朋友能看到我的改动吗？**
A：能。`push.bat` 推送后，朋友下次开工（`start.bat`）会自动下载到最新版本。

---

*文档维护：由项目所有者授权更新；朋友如有规则疑问请直接与所有者沟通。*